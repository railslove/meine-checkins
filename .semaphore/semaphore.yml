version: v1.0
name: WFD MasterApp CI
agent:
  machine:
    type: a1-standard-4
    os_image: macos-xcode12
blocks:
  - name: Install
    dependencies: []
    task:
      prologue:
        commands:
          - sem-version node 12
      jobs:
        - name: yarn install
          commands:
            - checkout
            - cache restore
            - yarn
            - cache store
  - name: Tests
    dependencies:
      - Install
    task:
      prologue:
        commands:
          - checkout --use-cache
          - cache restore
      jobs:
        - name: jest
          commands:
            - yarn test
  - name: Static Analysis
    dependencies:
      - Install
    task:
      prologue:
        commands:
          - checkout --use-cache
          - cache restore
      jobs:
        - name: lint
          commands:
            - yarn lint
        - name: typescript
          commands:
            - 'yarn check:types'
